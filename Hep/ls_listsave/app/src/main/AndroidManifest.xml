<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ls_listsave">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMS.excelActivity" />
        <activity android:name=".MainMS.todoActivity" />
        <activity android:name=".MainMS.settingActivity" />
        <activity android:name=".MainMS.homeActivity" />
        <activity android:name=".LocationList_RecyclerView.LocationList" />
        <activity
            android:name=".MainMS.MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--출처: https://duzi077.tistory.com/283 [키패드 눌러도 화면 고정]-->
        <activity
            android:name=".FullImage"
            android:label="FullImage" />
    </application>


    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
</manifest>